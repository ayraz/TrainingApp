<resources>

    <!-- Base application theme. -->
    <!-- Required inheritance for custom toolbar support. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/appBackground</item>
    </style>

    <style name="MainScreenButton" parent="">
        <item name="android:layout_width">200dp</item>
        <item name="android:layout_height">180dp</item>
        <item name="android:gravity">bottom|center</item>
        <item name="android:paddingTop">25dp</item>
        <item name="android:paddingBottom">25dp</item>
        <!--<item name="app:layout_constraintBottom_toBottomOf">parent</item>-->
        <!--<item name="app:layout_constraintTop_toTopOf">parent</item>-->
    </style>

    <style name="CustomDialog" parent="@style/Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">false</item>
    </style>

    <style name="AppTheme.CardButton" parent="Base.Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/white</item>
    </style>
</resources>
